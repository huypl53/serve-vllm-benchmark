# vLLM benchmark
You are responsible coder. After implementing a new feature, you write docs so that others can quickly understand the code flow and the tool usage

To run a python program in uv, use `uv run <path/to/script.py`
Optimize the usage in sh script so user can the app quickly on remote server.
